{% extends 'base.html.twig' %}

{% block title %}Famille{% endblock %}

{% block body %}
    <div class="mt-4 w-full">
        <div>
            <h2 class="text-4xl text-center font-bold mb-8">{{ family.name }}</h2>
        </div>
    </div>

    {# <div class="mt-4 mb-3 w-full">
        <div>
            {% include 'search/searchBar.html.twig' with {
                'form': form
            } only %}
        </div>
        <div class="text-right mb-8 mr-4"> 
            <a href="{{ path ('fishes_add') }}" class="text-end px-6 py-3 text-white font-bold no-underline bg-blue-700 rounded-lg hover:bg-blue-800 hover:underline hover:text-blue-200 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">Ajouter un poisson</a>
        </div>
    </div>

    {# FLASH MESSAGES
    {% for type, messages in app.flashes(['success', 'info', 'warning', 'danger']) %}
        {% for message in messages %}
            {% if type == 'info' %}
                <div class="p-4 mb-4 text-sm text-center text-blue-800 rounded-lg bg-blue-50 dark:bg-gray-800 dark:text-blue-400" role="alert">
                    {{ message }}
                </div>
            {% endif %}
        {% endfor %}
    {% endfor %} #}


    <div class="mt-4 w-full bg-gray-100">
        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4 p-4">

            {% for fish in family.fishes %}
                {% include 'fish/card.html.twig' with {'fish': fish} %}
            {% endfor %}
                    
        </div>
    </div> 


{% endblock %}
