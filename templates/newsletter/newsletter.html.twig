{% extends 'base.html.twig' %}

{% block body %}

  <h1 class="text-4xl font-bold text-center text-blue-800 dark:text-blue-400">Newsletter</h1>

  <div class="flex justify-center mt-10">
    <div class="w-full max-w-80">{{ form(newsletterForm) }}</div>
  </div>

{% endblock %}